# scripts/eval/

See [scripts/README.md](../README.md) for the overall scripts organization.

## What goes here

**eval scripts** - Policy evaluation scripts (eval_*.py) for evaluating bot policies on offline datasets.

## Migration plan (NOT YET MOVED)

Currently the candidate files are still at the project root.
This folder is reserved for the future move.

When ready:
```bash
git mv eval_*.py scripts/eval/  # where applicable
```
